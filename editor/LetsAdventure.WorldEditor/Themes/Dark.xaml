<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <SolidColorBrush x:Key="Theme.Window.Background" Color="#FF2D2D30" />
  <SolidColorBrush x:Key="Theme.Window.Foreground" Color="#FFF1F1F1" />
  <SolidColorBrush x:Key="Theme.Text.Muted" Color="#FFADADAD" />
  <SolidColorBrush x:Key="Theme.Border" Color="#FF555555" />
  <SolidColorBrush x:Key="Theme.Panel.Background" Color="#FF252526" />
  <SolidColorBrush x:Key="Theme.Control.Background" Color="#FF333337" />
  <SolidColorBrush x:Key="Theme.Log.Background" Color="#FF1E1E1E" />
  <SolidColorBrush x:Key="Theme.Log.Foreground" Color="#FFDCDCDC" />
  <SolidColorBrush x:Key="Theme.Button.Background" Color="#FF3F3F46" />
  <SolidColorBrush x:Key="Theme.Button.Foreground" Color="#FFF3F3F3" />
  <SolidColorBrush x:Key="Theme.Button.Border" Color="#FF656565" />
  <SolidColorBrush x:Key="Theme.Accent" Color="#FF007ACC" />
  <SolidColorBrush x:Key="Theme.ListItem.Selected" Color="#FF094771" />
  <Color x:Key="Theme.Viewport.ClearColor">#FF1E2A35</Color>
  <SolidColorBrush x:Key="Theme.Viewport.BorderBrush" Color="#FF3D5C73" />

  <Style TargetType="{x:Type Window}">
    <Setter Property="Background" Value="{DynamicResource Theme.Window.Background}" />
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
  </Style>

  <!-- Flat chrome so Background/Foreground setters actually apply (default templates ignore them). -->
  <Style TargetType="{x:Type Button}">
    <Setter Property="Background" Value="{DynamicResource Theme.Button.Background}" />
    <Setter Property="Foreground" Value="{DynamicResource Theme.Button.Foreground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Button.Border}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="10,6" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Button}">
          <Border x:Name="Bd"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="2"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Opacity" Value="0.92" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="Bd" Property="Opacity" Value="0.82" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Bd" Property="Opacity" Value="0.4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="{x:Type Label}">
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
  </Style>

  <Style TargetType="{x:Type TextBox}">
    <Setter Property="Background" Value="{DynamicResource Theme.Control.Background}" />
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
    <Setter Property="CaretBrush" Value="{DynamicResource Theme.Window.Foreground}" />
  </Style>

  <Style TargetType="{x:Type CheckBox}">
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
    <Setter Property="Background" Value="Transparent" />
  </Style>

  <Style TargetType="{x:Type RadioButton}">
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
    <Setter Property="Background" Value="Transparent" />
  </Style>

  <Style TargetType="{x:Type ListBox}">
    <Setter Property="Background" Value="{DynamicResource Theme.Control.Background}" />
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
  </Style>

  <Style TargetType="{x:Type ListBoxItem}">
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Padding" Value="4,2" />
    <Style.Triggers>
      <Trigger Property="IsSelected" Value="True">
        <Setter Property="Background" Value="{DynamicResource Theme.ListItem.Selected}" />
        <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
      </Trigger>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="IsSelected" Value="True" />
          <Condition Property="Selector.IsSelectionActive" Value="False" />
        </MultiTrigger.Conditions>
        <Setter Property="Background" Value="{DynamicResource Theme.Panel.Background}" />
      </MultiTrigger>
    </Style.Triggers>
  </Style>

  <Style TargetType="{x:Type ComboBox}">
    <Setter Property="Background" Value="{DynamicResource Theme.Control.Background}" />
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
    <Setter Property="ItemContainerStyle">
      <Setter.Value>
        <Style TargetType="{x:Type ComboBoxItem}">
          <Setter Property="Background" Value="{DynamicResource Theme.Control.Background}" />
          <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
          <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
              <Setter Property="Background" Value="{DynamicResource Theme.ListItem.Selected}" />
              <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
            </Trigger>
          </Style.Triggers>
        </Style>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="{x:Type TabControl}">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
  </Style>

  <Style TargetType="{x:Type TabItem}">
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
    <Setter Property="Background" Value="{DynamicResource Theme.Panel.Background}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
    <Setter Property="Padding" Value="12,6" />
  </Style>

  <Style TargetType="{x:Type GroupBox}">
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
  </Style>

  <Style TargetType="{x:Type StatusBar}">
    <Setter Property="Background" Value="{DynamicResource Theme.Panel.Background}" />
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
  </Style>

  <Style TargetType="{x:Type Menu}">
    <Setter Property="Background" Value="{DynamicResource Theme.Window.Background}" />
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
  </Style>

  <Style TargetType="{x:Type MenuItem}">
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
    <Setter Property="Background" Value="Transparent" />
  </Style>

  <Style TargetType="{x:Type ContextMenu}">
    <Setter Property="Background" Value="{DynamicResource Theme.Panel.Background}" />
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
  </Style>

  <Style TargetType="{x:Type Separator}">
    <Setter Property="Background" Value="{DynamicResource Theme.Border}" />
    <Setter Property="Margin" Value="2,4" />
  </Style>

  <Style TargetType="{x:Type ProgressBar}">
    <Setter Property="Foreground" Value="{DynamicResource Theme.Accent}" />
    <Setter Property="Background" Value="{DynamicResource Theme.Control.Background}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
  </Style>

  <Style TargetType="{x:Type Slider}">
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
  </Style>

  <Style TargetType="{x:Type ToolTip}">
    <Setter Property="Background" Value="{DynamicResource Theme.Panel.Background}" />
    <Setter Property="Foreground" Value="{DynamicResource Theme.Window.Foreground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
  </Style>

  <Style TargetType="{x:Type ScrollViewer}">
    <Setter Property="Background" Value="Transparent" />
  </Style>
</ResourceDictionary>
